general:
  branches:
    ignore:
      - gh-pages
dependencies:
  pre:
    - npm install -g broccoli-cli
test:
  override:
    - echo 'No tests yet'
deployment:
  master:
    branch: master
    commands:
      - ./deploy